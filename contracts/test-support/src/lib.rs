//! Mocks for tests only, never deployed: a token, the 8004 registries, and the
//! hostile hooks of docs/decisions/call-graph-on-soroban.md (one that panics,
//! one that exhausts the budget, one that returns the wrong type). Written in
//! B11 (#18).
#![no_std]
