# Upstream pins nightly-2025-08-11. This port builds with the toolchain
# docs/decisions/stellar-target.md pins for the rest of Square (PORTING.md).
[toolchain]
channel = "1.98.1"
targets = ["wasm32v1-none"]
components = ["rustfmt"]
